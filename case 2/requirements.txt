# Case 2 - Reality Gap Modeling & Trajectory Optimization.

# Analysis + modeling stack.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

plotly>=5.0                # analysis.py: the interactive target/actual/script viewer
scikit-learn>=1.3          # linear-regression baseline (and models to beat it)
scipy>=1.10                # optional: Butterworth filter for the jerk metric

torch>=2.0                 # the gap model, and the optimizer that differentiates it
tensorboard>=2.10          # train_distillation_model.py logs each run to runs/

# Only send.py's --engine ur_rtde (real-hardware .path streaming) needs this;
# everything else talks to the robot over raw TCP sockets, standard library only.
ur_rtde==1.6.5
